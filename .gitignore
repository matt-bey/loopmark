node_modules/
*.log
.DS_Store
coverage/
.vitest/
# dist/ is intentionally COMMITTED -- users grab the bookmarklet without a toolchain.

# Saved Loop pages used for local verification. These contain real, internal
# document content and must never be committed.
temp/
.scratch*

# Private scrub terms. See scripts/check-fixtures.mjs.
scrub.local.json
